function solution(babbling) {
    const str = ["aya", "ye", "woo", "ma"]; 
    var chk; 
    let answer = 0;
    for(let i=0; i<babbling.length; i++) { 
         chk = babbling[i]; 
         for(let l=0; l<str.length; l++) { 
            chk = chk.replace(str[l], );
         }

    }
    return answer;
}